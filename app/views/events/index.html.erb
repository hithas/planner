<h1>Events</h1>

<p><a href="/new_event">Add a New Event</a></p>

<table>
  <thead>
    <tr>
      <th>Start time</th>
      <th>End time</th>
      <th>Category</th>
      <th>Day</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
  <% @events.each do |event| %>
    <tr>
          <td><%= event.start_time %></td>
          <td><%= event.end_time %></td>
          <td><%= event.category_id %></td>
          <td><%= event.day_id %></td>
          <td><a href="/events/<%= event.id %>">Show</a>
        </td>
      <td><a href="/events/<%= event.id %>/edit">Edit</a></td>
      <td><a href="/events/<%= event.id %>/destroy">Destroy</a></td>
    </tr>
  <% end %>
  </tbody>
</table>

